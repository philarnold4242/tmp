from setuptools import setup, find_packages

setup(
    name='d2l',
    version='0.1.0',
    packages=find_packages(), # This automatically finds the 'd2l' directory
    # other metadata like author, description, install_requires, etc.
)
